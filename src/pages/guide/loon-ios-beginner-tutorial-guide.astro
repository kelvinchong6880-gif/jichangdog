---
import BaseLayout from '../../layouts/BaseLayout.astro';
const title = "Loon 使用教程：轻量且强大的 iOS 翻墙代理工具";
const description = "抛弃难用的圈X！带你深度解析 iOS 颜值天花板 Loon 代理工具，掌握一键插件 (Plugin) 安装、MITM HTTPS 抓包去广告与流媒体智能分流策略。";
---

<BaseLayout title={title} description={description}>
  <article class="page-shell knowledge-layout">
    <div class="article-content prose">
      <h1>{title}</h1>
      
      <p class="lead-paragraph"><strong>结论先行：Loon 是 iOS 端兼顾颜值与强大插件生态的综合最优解！</strong><br><br>在 2026 年的 iOS 翻墙生态中，很多新手被 Quantumult X (圈X) 极其繁琐的反人类代码和解析器狠狠劝退，又觉得 Shadowrocket (小火箭) 的经典列表界面过于简陋，而 Surge 高达 50 美元的售价更是让人望而却步。<br><strong>Loon ($5.99)</strong> 正好完美填补了这片黄金真空地带——它既有极其直观、充满现代果味的图形化 UI 界面，又在底层原生地集成了媲美 Surge 的 <strong>强大插件 (Plugin) 体系</strong>。更重要的是，它对 <strong>VLESS、Reality 和 Hysteria 2</strong> 等最新抗封锁协议支持得极速且完美！<br><br>小白快速上手只需 3 步极简操作：<br>1. 打开 Loon，进入底部的 <strong>【配置】</strong> 页面，找到 <strong>【节点订阅 (Subscription)】</strong> ➔ 点击右上角“+”号。<br>2. 输入一个好记的标签（如机场名称），在 URL 栏粘贴机场提供的通用订阅链接，点击保存并等待下载完毕。<br>3. 返回首页点击右上角的全局开关开启代理（首次需允许安装系统底层 VPN 描述文件），在策略组中选中有效节点，即可秒开全球网络，丝滑无比。</p>

      <h2 id="client-comparison">1. 生态对决：iOS 四大神器横向对比，为什么 Loon 是新星？</h2>
      <p>工具的价值取决于你的需求。以下对比表将帮助你理清 Loon 在苹果生态中的独特护城河与不可替代的价值：</p>
      
      <div class="table-responsive">
        <table>
          <thead>
            <tr>
              <th>客户端名称</th>
              <th>官方定价</th>
              <th>UI 颜值与上手度</th>
              <th>插件与扩展能力</th>
              <th>机场狗 2026 最终评价</th>
            </tr>
          </thead>
          <tbody>
            <tr>
              <td><strong>Loon</strong></td>
              <td><strong>$5.99</strong></td>
              <td><strong>极高。</strong>图形化交互设计，界面高度现代化，动画流畅。</td>
              <td><strong>极强。</strong>拥有专属的 Plugin 一键安装体系，彻底告别繁琐代码。</td>
              <td>🌟 <strong>2026 iOS 综合体验天花板</strong>，颜值党与极客兼修，强烈推荐。</td>
            </tr>
            <tr>
              <td><strong>Shadowrocket (小火箭)</strong></td>
              <td>$2.99</td>
              <td>较低。偏向传统系统设置列表风格，视觉陈旧。</td>
              <td>基础。能满足日常分流，但缺乏高级重写机制。</td>
              <td>高性价比装机必备，预算有限小白的首选。</td>
            </tr>
            <tr>
              <td><strong>Quantumult X (圈X)</strong></td>
              <td>$7.99</td>
              <td>优秀。但由于底层数据结构反人类，配置门槛极高。</td>
              <td>极强。依赖复杂的本地 JS 脚本与资源解析器拼接。</td>
              <td>协议严重断代（不支持新协议），新手极度不推荐。</td>
            </tr>
            <tr>
              <td><strong>Surge iOS</strong></td>
              <td>$49.99 起</td>
              <td>顶奢。极简且专业的企业级仪表盘。</td>
              <td>巅峰。模块化生态与极致的底层网络协议调试。</td>
              <td>预算极其充裕的高净值玩家和 iOS 开发者的最终归宿。</td>
            </tr>
          </tbody>
        </table>
      </div>

      <h2 id="tech-deep-dive">2. 极客进阶：榨干 Loon 底层引擎的黑科技架构与高级配置</h2>
      <p>只用 Loon 来接管节点测速，是对这 5.99 美金的巨大侮辱。作为一款轻量却内核狂暴的代理引擎，你必须掌握以下几个高级模块，才能让你的 iPhone 真正进入“全自动化与绝对无广告”的数字净土。</p>
      
      <h3>文本配置架构：透视 <code>.conf</code> 结构与 Sub-rule 子规则</h3>
      <p>虽然 Loon 拥有极度友好的图形化 UI，但在其底层，所有的设置都对应着一个极其工整的文本配置文件。很多从 Surge 迁移过来的老玩家会发现，Loon 的配置结构（如 <code>[General]</code>, <code>[Proxy]</code>, <code>[Rule]</code>）几乎与 Surge 一脉相承。<br>Loon 最强大的底层规则机制之一是 <strong>子规则集 (Sub-rule)</strong>。在面对数量高达几十万条的广告域名库时，如果全部写死在主配置里，会导致软件占用海量内存并卡死。极客的做法是：在“分流规则”中，直接订阅远端的纯文本域名集。例如：</p>
      <pre><code>[Rule]
SUB-RULE,https://raw.githubusercontent.com/example/anti-ad/anti-ad-loon.txt,REJECT
SUB-RULE,https://raw.githubusercontent.com/example/apple-services.txt,DIRECT
GEOIP,CN,DIRECT
FINAL,PROXY</code></pre>
      <p>Loon 的底层会利用高效的 <strong>Radix Tree (基数树) 算法</strong>，将几十万条域名在后台静默加载到内存树中，匹配耗时不到 1 毫秒！这种极致的性能压榨，是小火箭完全无法企及的。</p>

      <h3>生态降维打击：Loon 插件体系 (Plugins) 与 JavaScript 重写</h3>
      <p>在圈X中，想去除 YouTube 广告或者解锁 Spotify，你需要在一堆英文配置文件里分别找到 `Rewrite`、`MITM` 和 `Script` 区块，分次粘贴不同的 URL，一步搞错就会导致全盘崩溃。<br>而 Loon 开创了极其优雅的 <strong>插件 (Plugin) 体系</strong>。大佬们把所有的 JS 拦截代码、域名正则和主机名列表打包成了一个单独的 <code>.plugin</code> 文本文件。你只需要在 Loon 底部的【配置】->【插件】中，点击右上角的“+”号，粘贴链接即可一键安装。<br>不仅如此，如果你有 JS 基础，你可以自己编写一段神级脚本，直接篡改 App 的网络响应体，实现“本地 VIP 破解”。例如，一段标准的 Loon JS 响应体重写代码如下：</p>
      <pre is:raw><code>// 拦截特定 API 响应并修改 JSON 体
let body = $response.body;
let obj = JSON.parse(body);
obj.data.is_vip = true;
obj.data.vip_expire = "2099-12-31";
$done({body: JSON.stringify(obj)});</code></pre>
      <p>结合 Loon 的插件指令 <code>http-response ^https://api\.example\.com/user/info script-path=vip.js, requires-body=true</code>，当 App 向服务器请求会员状态时，Loon 会在底层把返回的 <code>false</code> 强制改成 <code>true</code>，从而直接解锁高级功能！</p>

      <h3>HTTPS 流量透视：MITM 证书配置、解密原理与 HTTP/2 支持</h3>
      <p>Loon 不仅仅是个代理工具，它还是一个 <strong>中间人攻击 (Man-In-The-Middle) 网络调试器</strong>。现在的 App 广告全部都走 HTTPS 加密，想要去广告或篡改 VIP 状态，就必须让 Loon 获得“查看加密流量”的最高权限。<br>1. 进入 Loon 的【配置】，找到 <strong>MITM (中间人解密)</strong>，点击生成一个新的本地 CA 证书并下载。<br>2. 打开 iPhone 的 <code>设置 -> 通用 -> 关于本机 -> 证书信任设置</code>，找到那个 Loon 生成的证书，手动打上绿色信任开关。<br>完成这步系统级提权后，Loon 就能在底层实时解密流经本地的 HTTPS 请求。值得一提的是，2026 年最新版的 Loon 已经原生支持了 <strong>HTTP/2 协议的 MITM 解密</strong>，这意味着它能拦截比以往更先进、并发更高的流媒体与头部大厂 App 流量，这是很多老牌代理软件难以做到的。</p>

      <h3>后台自动化王者：Cron 定时任务与 BoxJS 联动</h3>
      <p>这是让 Loon 直接封神的另一大杀器。Loon 完全兼容了圈X赖以成名的 <strong>定时任务引擎 (Cron Task)</strong>。<br>利用 <strong>BoxJS</strong>（一个依托于代理层运行的全局键值对环境），你可以用 MITM 抓取京东或 B 站的 Cookie 存入本地数据库。然后在 Loon 的【任务】面板中，写下一条类似 Unix 服务器的定时指令：</p>
      <pre><code>cron "0 9 * * *" script-path=https://example.com/jd_sign.js, tag=京东自动签到</code></pre>
      <p>设定完成后，Loon 就会在每天早上 9 点，即使你没有打开 App，也会自动在后台利用网络引擎发起 HTTP 请求，帮你完成领京豆和 B 站投币等全自动化操作。你相当于在 iPhone 里塞进了一台不会休眠的小型 Linux 服务器！</p>

      <h3>网络极客的利器：捕获调试 (Capture) 与流量抓包</h3>
      <p>Loon 内置了极其专业的流量嗅探器。在主页点击 <strong>捕获 (Capture)</strong> 开启后，Loon 会像电脑上的 Fiddler 或 Charles 一样，拦截并记录手机上所有 App 发出的网络请求。<br>通过查看实时的 HTTP Header、Cookie 以及 JSON 响应体，极客玩家可以轻松逆向找出流氓软件后台发送隐私数据（如手机 IMEI、剪贴板内容）的 API 接口，并直接在 Loon 里写一条 <code>REJECT (拒绝)</code> 规则，将其彻底斩杀于系统底层！</p>

      <h2 id="qa-faq">3. 深度极客排障与高频报错解答 (Q&A)</h2>
      <p>即便是设计再优秀的软件，在面临国内复杂的局域网环境和老旧的机场协议时，也难免遇到卡壳。以下是新手在使用 Loon 时最高频的报错排障指南：</p>

      <div class="faq-item">
        <h3>Q1: Loon 导入订阅提示“解析订阅失败 (Parse Error)”，或者节点列表全是空乱码？</h3>
        <p><strong>答：</strong>这说明你的机场客服极不专业，提供的是只适配古早版 Clash 的 YAML 格式，或者干脆给了一个多层嵌套加密的 Base64 乱码，超出了 Loon 原生解析器的容错范围。<br><strong>破局解法：</strong>千万不要在机场后台乱点“复制 Clash 链接”。请寻找 <strong>“复制通用订阅”、“V2ray 订阅”</strong> 或 <strong>“SS/Trojan 订阅”</strong> 粘贴到 Loon 里。<br>如果这家机场实在落后只给 Clash 链接，你可以利用第三方开源的 <code>Subconverter (订阅转换 API)</code>。比如在你的链接前加上 <code>https://sub.xeton.dev/sub?target=loon&url=</code>，将机场的链接强行在云端转化为 Loon 专属的配置文件格式，再导入进去就能瞬间满血复活。</p>
      </div>

      <div class="faq-item">
        <h3>Q2: 为什么 Loon 开启后，节点测速全部超时 (Timeout)，国内 App 正常，但连 Google 都打不开？</h3>
        <p><strong>答：</strong>这是典型的 <strong>DNS 污染或代理模式死锁</strong> 报错。最常见的原因是你的系统时间没对准，导致加密握手失败。<br><strong>解法：</strong>第一步，请确保 iPhone 设置里的“系统时间”处于 <strong>自动设置</strong> 状态。VLESS/Reality 等现代协议对时间差极其敏感，相差 30 秒就会强制拒绝连接。<br>第二步，在 Loon 首页检查当前的 <strong>路由模式</strong>。一定要选 <code>规则 (Rule)</code> 模式！如果你选了 <code>全局代理 (Global)</code>，且恰好当前策略组选中了一个超时的死节点，那么所有的国内外流量（包括国内的微信、支付宝）都会被堵在这个死节点上，导致全局断网发热。</p>
      </div>

      <div class="faq-item">
        <h3>Q3: 安装了牛逼的去广告插件 (Plugin)，为什么提示“MITM 未启用”或者开屏广告依然顽固存在？</h3>
        <p><strong>答：</strong>Loon 的插件体系要发挥去广告的威力，100% 依赖于 <strong>HTTPS 流量解密</strong>。<br>如果你只安装了插件，却没有去系统 <code>设置 -> 关于本机 -> 证书信任设置</code> 里给 Loon 的 CA 证书打上绿勾，iOS 底层的安全机制会死死防住 Loon，导致插件只能看到加密后的乱码，自然无法去广告。<br>另外，针对 YouTube 这种采用 <strong>QUIC 协议 (基于 UDP 443)</strong> 的流媒体，MITM 是抓不到包的。你必须在规则中加入一条 <code>REJECT, no-resolve</code> 阻断 UDP 443 端口，强迫 YouTube 降级回 TCP，再去广告才能成功。</p>
      </div>
      
      <div class="faq-item">
        <h3>Q4: 为什么大家都在推荐买 Loon 代替圈X，圈X 真的不行了吗？</h3>
        <p><strong>答：</strong>从底层技术迭代来说，是的。Quantumult X 的作者更新频率极低，且因为固执的开发理念，至今原生不支持 VLESS、Reality、Hysteria 2 等 2026 年防 GFW 封锁的核心前沿协议。这导致当机场为了抗封锁全线升级新协议时，圈X 用户将面临无节点可用的绝境。<br>而 Loon 的开发者极其活跃，几乎第一时间原生跟进了所有现代协议，同时在 UI 交互和插件生态的易用性上形成了绝对的降维打击。放弃旧时代的圈X，拥抱次世代的 Loon，是当下最明智的极客选择。</p>
      </div>

      <h2 id="conclusion-and-recommendation">4. 结语：让绝美的引擎，跑在最极致的专线之上</h2>
      <p>当你斥资 $5.99 买下 Loon，你不仅买到了当今 iOS 平台上颜值最高的代理工具，更获得了一个支持最新协议引擎、完美解耦的插件中心以及强大的底层网络调试器。</p>
      <p><strong>但是，引擎再华丽，没有顶级的网络物理链路支撑，也只是空中楼阁。</strong></p>
      <p>如果你给 Loon 导入的是几十块钱一年、天天被 GFW 疯狂丢包的公网直连机场，每当晚高峰来临，无论 Loon 的 <code>url-test</code> 策略组怎么拼命测试，无论它的插件写得多么优雅，你连一张 1080P 的图片都刷不出来。因为底层跨国物理海缆的拥堵，是任何软件层面都无法解决的物理法则。</p>
      <p>想要真正释放 Loon 引擎的极致流畅度，体验什么叫做“全球网络，点击即秒开”，你必须搭配 <strong>全链路底层干净、端到端零丢包，且原生完美适配最新协议的 IPLC/IEPL 国际专线</strong>。</p>
      <p>强烈建议您直接访问我们的 <strong><a href="/recommend/">2026 高端专线机场推荐榜单</a></strong>。榜单中的顶级供应商，不仅提供纯正的高端物理专线，更能与 Loon 的智能分流体系完美契合。告别折腾与晚高峰断流，让你的 iPhone 真正享受全天候零延时、丝滑如初的顶级赛博冲浪体验！</p>
    
        <blockquote style="background: #f8fafc; border-left: 4px solid #10b981; padding: 20px; border-radius: 8px; margin: 40px 0; font-size: 1.05rem; box-shadow: 0 4px 15px rgba(16, 185, 129, 0.05);">
          <h4 style="margin-top: 0; color: #047857; font-size: 1.15rem; margin-bottom: 12px; display: flex; align-items: center; gap: 8px;">
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"></path><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"></path></svg>
            更多极客进阶教程
          </h4>
          <p style="margin-bottom: 10px; color: #475569;">如果您正在折腾其他客户端或者遇到网络连通性问题，不妨参考我们的其他干货教程，帮您排雷避坑：</p>
          <ul style="margin-bottom: 0; padding-left: 20px;">
            <li style="margin-bottom: 8px;"><a href="/guide/netflix-signup-and-full-unlock-guide/" style="color: #059669; font-weight: 600; text-decoration: none;">Netflix 奈飞账号注册与机场节点解锁全指南（如何看非自制剧） ↗</a></li>
            <li><a href="/guide/android-tv-v2rayng-youtube-setup-guide/" style="color: #059669; font-weight: 600; text-decoration: none;">Android TV / 安卓电视盒子安装 v2rayNG 翻墙看油管教程 ↗</a></li>
          </ul>
        </blockquote>

    </div>
    
    <aside class="toc-sidebar">
      <div class="toc-container">
        <h3 class="toc-title">本文目录</h3>
        <ul id="toc-list" class="toc-list">
          <!-- JS will populate this -->
        </ul>
      </div>
    </aside>
  </article>

  <style>
    .knowledge-layout {
      display: flex;
      gap: 40px;
      padding-top: 140px;
      padding-bottom: 80px;
      max-width: 1200px;
      margin: 0 auto;
      align-items: flex-start;
    }
    .article-content {
      flex: 1;
      min-width: 0;
      background: #fff;
      padding: 40px;
      border-radius: 16px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.03);
    }
    
    /* Typography / Prose Styles */
    .prose h1 {
      font-size: 2rem;
      color: var(--ink);
      line-height: 1.4;
      margin-bottom: 24px;
    }
    .prose .lead-paragraph {
      font-size: 1.1rem;
      color: #374151;
      background: #eff6ff;
      border-left: 4px solid #3b82f6;
      padding: 16px 20px;
      margin-bottom: 32px;
      border-radius: 0 8px 8px 0;
      line-height: 1.6;
    }
    .prose h2 {
      font-size: 1.5rem;
      color: var(--blue);
      margin-top: 48px;
      margin-bottom: 20px;
      border-bottom: 2px solid #eff4ff;
      padding-bottom: 8px;
    }
    .prose h3 {
      font-size: 1.25rem;
      color: #1f2937;
      margin-top: 32px;
      margin-bottom: 16px;
    }
    .prose p {
      font-size: 1rem;
      line-height: 1.8;
      color: #4b5563;
      margin-bottom: 20px;
    }
    .prose a {
      color: var(--blue);
      text-decoration: none;
      font-weight: 600;
    }
    .prose a:hover {
      text-decoration: underline;
    }
    .prose ul, .prose ol {
      margin-bottom: 24px;
      padding-left: 24px;
    }
    .prose ul li, .prose ol li {
      margin-bottom: 12px;
      line-height: 1.6;
      color: #4b5563;
    }
    .prose pre {
      background: #1f2937;
      color: #f3f4f6;
      padding: 16px;
      border-radius: 8px;
      overflow-x: auto;
      margin-bottom: 24px;
    }
    .prose code {
      font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, monospace;
      font-size: 0.9em;
      background: #f3f4f6;
      padding: 2px 6px;
      border-radius: 4px;
      color: #ef4444;
    }
    .prose pre code {
      background: transparent;
      padding: 0;
      color: inherit;
    }
    
    /* Table Styles */
    .table-responsive {
      overflow-x: auto;
      margin-bottom: 40px;
    }
    .prose table {
      width: 100%;
      border-collapse: collapse;
      font-size: 0.95rem;
    }
    .prose th, .prose td {
      border: 1px solid #e5e7eb;
      padding: 14px 16px;
      text-align: left;
    }
    .prose th {
      background: #f9fafb;
      color: var(--ink);
      font-weight: 600;
    }
    .prose tbody tr:nth-child(even) {
      background: #fcfcfc;
    }

    /* FAQ Styles */
    .faq-item {
      background: #fcfcfc;
      border: 1px solid #f3f4f6;
      border-radius: 12px;
      padding: 24px;
      margin-bottom: 24px;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.02);
    }
    .faq-item h3 {
      color: var(--blue);
      font-size: 1.15rem;
      margin-top: 0;
      margin-bottom: 12px;
      display: flex;
      align-items: flex-start;
      gap: 8px;
    }
    .faq-item p {
      margin-bottom: 0;
      color: #4b5563;
      line-height: 1.7;
    }
    
    /* TOC Sidebar Styles */
    .toc-sidebar {
      width: 280px;
      flex-shrink: 0;
      position: sticky;
      top: 100px;
    }
    .toc-container {
      background: #fff;
      padding: 24px;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0,0,0,0.03);
      border: 1px solid rgba(0,0,0,0.05);
    }
    .toc-title {
      font-size: 1.1rem;
      font-weight: 700;
      color: var(--ink);
      margin-bottom: 16px;
      padding-bottom: 12px;
      border-bottom: 1px solid var(--line);
    }
    .toc-list {
      list-style: none;
      padding: 0;
      margin: 0;
      display: flex;
      flex-direction: column;
      gap: 12px;
    }
    .toc-link {
      color: var(--muted);
      text-decoration: none;
      font-size: 0.9rem;
      transition: all 0.2s;
      display: block;
      line-height: 1.4;
      border-left: 2px solid transparent;
      padding-left: 10px;
      margin-left: 2px;
    }
    .toc-link:hover, .toc-link.active {
      color: var(--blue);
      font-weight: 600;
      border-left-color: var(--blue);
    }

    @media (max-width: 992px) {
      .knowledge-layout {
        flex-direction: column;
      }
      .toc-sidebar {
        display: none; 
      }
      .article-content {
        padding: 24px;
      }
    }
  </style>

  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const content = document.querySelector('.article-content');
      const tocList = document.getElementById('toc-list');
      if (!content || !tocList) return;

      const headings = content.querySelectorAll('h2');
      
      headings.forEach((heading) => {
        if(!heading.id) return;
        const li = document.createElement('li');
        const a = document.createElement('a');
        a.href = '#' + heading.id;
        a.textContent = heading.textContent;
        a.className = 'toc-link';
        li.appendChild(a);
        tocList.appendChild(li);
      });

      const observerOptions = {
        root: null,
        rootMargin: '-100px 0px -60% 0px',
        threshold: 0
      };

      const observer = new IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            const id = entry.target.getAttribute('id');
            document.querySelectorAll('.toc-link').forEach(link => {
              link.classList.remove('active');
              if (link.getAttribute('href') === '#' + id) {
                link.classList.add('active');
              }
            });
          }
        });
      }, observerOptions);

      headings.forEach(heading => observer.observe(heading));
    });
  </script>
</BaseLayout>
