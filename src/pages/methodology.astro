---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout 
  title="机场测评方法论与测速原则：客观、透明、可复现的评测基准 | 机场狗"
  description="深入了解机场狗的完整机场测评方法论。我们坚持独立测试，所有机场节点数据均基于真实宽带环境测速，公开测试条件与网络环境，为用户提供透明、客观的科学上网服务商数据支持。"
>
  <article class="page-shell knowledge-layout">
    <div class="article-content prose">
      <div class="document-header">
        <p class="eyebrow">独立整理 · 持续更新</p>
        <h1>机场狗测评方法论：每个结论，都应该能找到依据</h1>
        <p class="lead-paragraph">在本站，我们认为所有的机场测速和测评结论都不应当是主观臆断。我们建立了一套完整、透明、且可复现的测试标准，确保你看到的每一组数据都是在特定真实环境下的客观记录。</p>
      </div>

      <h2 id="section-1">1. 测评数据的来源与核验原则</h2>
      <p>在撰写任何一篇<strong>机场测评</strong>文章之前，我们会通过以下几个独立的信息源来交叉比对数据，以确保最终呈现在用户面前的信息具有极高的可信度：</p>
      <ul>
        <li><strong>服务商公开资料：</strong>提取官方网站标注的线路类型（如 IPLC 专线、IEPL、中转节点等）、带宽上限、流媒体解锁保证以及退款协议。</li>
        <li><strong>后台节点快照：</strong>我们会实际购买套餐并登录后台，获取节点列表的真实快照。对于标榜“全专线”的机场，我们会抽查其入口与出口 IP 的自治系统号码（ASN）以核实线路真实性。</li>
        <li><strong>独立网络测试：</strong>所有被称为“本站实测”的数据，都必须附带明确的测试环境记录（包括测试时间、本地宽带运营商、使用的代理客户端类型）。</li>
      </ul>

      <h2 id="section-2">2. 节点测速标准（Speed Test Protocol）</h2>
      <p>网速是评价一个机场最直观的标准，但也是最容易被“环境因素”干扰的数据。为了满足对数据严谨性的规范（如符合 Bing 搜索引擎的高质量内容要求），我们采用了标准化的<strong>机场测速</strong>流程：</p>
      <h3 id="section-2-1">2.1 测试环境控制</h3>
      <p>我们的标准测试环境默认采用中国大陆三大运营商（电信、联通、移动）的千兆（1000Mbps）家庭宽带。测试设备统一使用有线网络连接，以排除本地 Wi-Fi 路由器性能或无线信号干扰带来的误差。</p>
      <h3 id="section-2-2">2.2 测速工具与目标节点</h3>
      <p>我们在测速时通常不使用单一的测速软件。我们结合使用 <strong>Speedtest.net</strong>（选择就近的国际目标服务器）以及 YouTube 4K/8K 视频的 <strong>Stats for nerds</strong> 实际连接速度。节点测试通常覆盖：</p>
      <ul>
        <li><strong>香港（HK）：</strong>作为亚太地区的主力节点，重点测试延迟与峰值带宽。</li>
        <li><strong>日本（JP）与新加坡（SG）：</strong>测试在亚太地区的路由优化与晚高峰拥堵情况。</li>
        <li><strong>美国（US）：</strong>测试跨太平洋海底光缆的连通性与丢包率。</li>
      </ul>

      <h2 id="section-3">3. 流媒体与 AI 解锁测试（Streaming & AI Access）</h2>
      <p>针对目前用户高度关注的 Netflix、Disney+ 以及 ChatGPT、Gemini 等 AI 服务的解锁能力，我们实行严格的“抽样验证”机制。</p>
      <p>我们不仅测试节点是否能够成功打开对应的网站主页，还会实际登录账号进行播放或交互测试，以区分“DNS 解锁”和“原生 IP 解锁”的稳定性差异。如果某个机场频繁出现流媒体解锁失效，我们会在该机场的评测卡片中明确标注警告信息。</p>

      <h2 id="section-4">4. 跑路与高风险预警机制</h2>
      <p>本站的 <a href="/#runaway-list">跑路机场黑名单</a> 是我们极为重视的板块。评价一个服务商是否处于高风险状态，我们主要考量以下几个指标：</p>
      <ul>
        <li><strong>节点存活率骤降：</strong>如果在连续 7 天内，核心节点大面积离线且无任何官方维护公告。</li>
        <li><strong>客服失联：</strong>通过官网工单或 Telegram 客服群发送消息，超过 72 小时无人工响应。</li>
        <li><strong>支付网关异常：</strong>仅支持高风险数字货币付款，或频繁更换个人收款码，没有企业级支付通道。</li>
        <li><strong>超低价倾销：</strong>推出严重低于市场成本价（如 10元/年，不限量）的“传家宝”套餐，这通常是服务商准备资金盘跑路的前兆。</li>
      </ul>

      <h2 id="section-5">5. 声明与免责</h2>
      <p>我们的评测方法论虽然严谨，但网络环境具有极强的区域性和时效性。<strong>我们所记录的数据仅代表测试当天的真实情况，不能作为对服务商未来稳定性的永久保证。</strong> 我们强烈建议用户在购买长期套餐（如年付）前，先通过月付套餐在自己的真实网络环境下进行小额体验。</p>
      <p>如果你发现我们的测速数据与你的实际体验存在巨大偏差，欢迎通过更正机制与我们联系。我们将对该服务商进行复测，并在必要时更新评测结果。</p>
    </div>

    <aside class="toc-sidebar">
      <div class="toc-container">
        <h3 class="toc-title">本文目录</h3>
        <ul id="toc-list" class="toc-list">
          <!-- JS will populate this -->
        </ul>
      </div>
    </aside>
  </article>
</BaseLayout>

<style is:global>
  .knowledge-layout {
    display: flex !important;
    flex-direction: row !important;
    gap: 40px;
    padding-top: 80px;
    padding-bottom: 80px;
    max-width: 1200px;
    margin: 0 auto;
    align-items: flex-start;
  }
  .article-content {
    flex: 1 1 0%;
    min-width: 0;
    background: #fff;
    padding: 40px;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.03);
    border: 1px solid #e5e7eb;
  }
  
  .document-header {
    text-align: center;
    margin-bottom: 40px;
  }
  .document-header .eyebrow {
    color: var(--blue);
    font-weight: 700;
    margin-bottom: 8px;
    font-size: 0.9rem;
  }
  .document-header h1 {
    font-size: 2.4rem;
    font-weight: 900;
    color: var(--ink);
    margin: 0 0 24px 0;
    line-height: 1.3;
  }
  
  .prose .lead-paragraph {
    font-size: 1.1rem;
    color: #374151;
    background: #f0fdf4;
    border-left: 4px solid #10b981;
    padding: 16px 20px;
    margin-bottom: 32px;
    border-radius: 0 8px 8px 0;
    line-height: 1.6;
    text-align: left;
  }
  .prose h2 {
    font-size: 1.6rem;
    color: var(--blue);
    margin-top: 48px;
    margin-bottom: 20px;
    border-bottom: 2px solid #eff4ff;
    padding-bottom: 8px;
  }
  .prose h3 {
    font-size: 1.25rem;
    color: #1f2937;
    margin-top: 32px;
    margin-bottom: 16px;
  }
  .prose p {
    font-size: 1rem;
    line-height: 1.8;
    color: #4b5563;
    margin-bottom: 20px;
  }
  .prose a {
    color: var(--blue);
    text-decoration: none;
    font-weight: 600;
  }
  .prose a:hover {
    text-decoration: underline;
  }
  .prose ul {
    margin-bottom: 24px;
    padding-left: 24px;
  }
  .prose ul li {
    margin-bottom: 12px;
    line-height: 1.6;
    color: #4b5563;
  }
  
  /* TOC Sidebar Styles */
  .toc-sidebar {
    width: 280px !important;
    flex-shrink: 0 !important;
    position: sticky !important;
    top: 100px !important;
  }
  .toc-container {
    background: #fff;
    padding: 24px;
    border-radius: 12px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.03);
    border: 1px solid rgba(0,0,0,0.05);
  }
  .toc-title {
    font-size: 1.1rem;
    font-weight: 700;
    color: var(--ink);
    margin: 0 0 16px 0;
    padding-bottom: 12px;
    border-bottom: 1px solid var(--line);
  }
  .toc-list {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }
  .toc-link {
    color: var(--muted);
    text-decoration: none;
    font-size: 0.9rem;
    transition: all 0.2s;
    display: block;
    line-height: 1.4;
    border-left: 2px solid transparent;
    padding-left: 10px;
    margin-left: 2px;
  }
  .toc-link:hover, .toc-link.active {
    color: var(--blue) !important;
    font-weight: 600;
    border-left-color: var(--blue) !important;
  }
  
  @media (max-width: 992px) {
    .knowledge-layout {
      flex-direction: column !important;
    }
    .toc-sidebar {
      display: none !important;
    }
    .article-content {
      padding: 24px;
    }
    .document-header h1 {
      font-size: 1.8rem;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const content = document.querySelector('.article-content');
    const tocList = document.getElementById('toc-list');
    if (!content || !tocList) return;

    const headings = content.querySelectorAll('h2');
    
    headings.forEach((heading) => {
      if(!heading.id) return;
      const li = document.createElement('li');
      const a = document.createElement('a');
      a.href = '#' + heading.id;
      a.textContent = heading.textContent;
      a.className = 'toc-link';
      li.appendChild(a);
      tocList.appendChild(li);
    });

    // Intersection Observer for highlighting active TOC link
    const observerOptions = {
      root: null,
      rootMargin: '-100px 0px -60% 0px',
      threshold: 0
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const id = entry.target.getAttribute('id');
          document.querySelectorAll('.toc-link').forEach(link => {
            link.classList.remove('active');
            if (link.getAttribute('href') === '#' + id) {
              link.classList.add('active');
            }
          });
        }
      });
    }, observerOptions);

    headings.forEach(heading => observer.observe(heading));
  });
</script>
